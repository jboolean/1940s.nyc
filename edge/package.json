{
  "name": "fourtiesnyc-edge",
  "version": "1.0.0",
  "description": "",
  "main": "compressTileAtEdge.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "rm -rf node_modules/sharp && npm install --arch=x64 --platform=linux sharp && serverless deploy --stage production"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless": "^2.13.0",
    "serverless-lambda-edge-pre-existing-cloudfront": "^1.1.5"
  },
  "dependencies": {
    "axios": "^0.21.0",
    "sharp": "^0.26.3"
  }
}
