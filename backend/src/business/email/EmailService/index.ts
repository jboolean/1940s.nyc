import PostmarkEmailService from './PostmarkEmailService';

import { TemplatedEmailData, EmailResult } from './EmailService';

export { TemplatedEmailData, EmailResult };

// This is where we could exchange Postmark for another email service implementation

export default PostmarkEmailService;
